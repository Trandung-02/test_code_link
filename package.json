{
  "name": "code-repo-root",
  "private": true,
  "description": "Chỉ là gốc Git — Next.js chạy trong Code_link (Vercel dùng installCommand/buildCommand trong vercel.json).",
  "scripts": {
    "build": "cd Code_link && npm run build",
    "dev": "cd Code_link && npm run dev",
    "start": "cd Code_link && npm run start",
    "lint": "cd Code_link && npm run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
